<ul reversed class="postlist" style="counter-reset: start-from {{ (postslistCounter or postslist.length) + 1 }}">
{% for post in postslist | reverse %}
  <li class="paper postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
    <a href="{{ post.url | url }}" class="postlist-link">
      <h2>
        {% if post.data.title %}
          {{ post.data.title }}
        {% else %}
          <code>{{ post.url }}</code>
        {% endif %}
      </h2>
    <p> <img width=200 alt="Image of compass rose" src="{{ post.data.image }}"/></p>
    <p>{{ post.data.description }}</p>
<!--    <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">Updated: {{ post.date | readableDate }}</time> -->
    
   <!--  {% for tag in post.data.tags %}
      {%- if collections.tagList.indexOf(tag) != -1 -%}
      {% set tagUrl %}/tags/{{ tag }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="post-tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %} -->
    </a>

  </li>
{% endfor %}
</ul>
